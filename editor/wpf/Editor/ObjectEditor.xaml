<UserControl x:Class="Editor.ObjectEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Background="{DynamicResource {x:Static SystemColors.GradientActiveCaptionBrushKey}}">
    <StackPanel>
        <Grid x:Name="m_grid" Background="{DynamicResource DarkBackgroundBrush}" ShowGridLines="True" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
        </Grid>
        <Label x:Name="m_label" HorizontalAlignment="Center" Content="Struct Instance" VerticalAlignment="Center" VerticalContentAlignment="Center" Height="20"/>
    </StackPanel>
</UserControl>
