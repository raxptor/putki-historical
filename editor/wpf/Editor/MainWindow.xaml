<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:local="clr-namespace:Editor" x:Class="Editor.MainWindow"
        Title="SuperBerit" Height="871" Width="859">
    <Grid>
            <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300">
             </ColumnDefinition>
            <ColumnDefinition Width="*">                
            </ColumnDefinition> 
        </Grid.ColumnDefinitions>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition Height="25"></RowDefinition>
            </Grid.RowDefinitions>                
            <local:FileBrowser x:Name="m_fileBrowser" Grid.Column="0" HorizontalAlignment="Stretch" />
            <Button Grid.Row="2" Height="25" Content="New Object" Click="Button_Click_NewObject"></Button>
        </Grid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition Height="25"></RowDefinition>
            </Grid.RowDefinitions>
            <local:ObjectEditor x:Name="m_objectEditor" Background="{DynamicResource DarkBackgroundBrush}" />            
            <Button Grid.Row="2" Height="25" Content="Save" Click="Button_Click_Save"></Button>
        </Grid>

    </Grid>
</Window>
